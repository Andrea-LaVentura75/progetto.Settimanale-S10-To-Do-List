import { AnimazioneDirective } from './animazione.directive';

describe('AnimazioneDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimazioneDirective();
    expect(directive).toBeTruthy();
  });
});
